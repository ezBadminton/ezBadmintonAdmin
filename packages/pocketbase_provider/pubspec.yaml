name: pocketbase_provider
description: Dart package which acts as a PocketBase data provider by exposing singleton.
publish_to: none

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
 pocketbase: ^0.16.0

dev_dependencies:
  very_good_analysis: ^5.1.0