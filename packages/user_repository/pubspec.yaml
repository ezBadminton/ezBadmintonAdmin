name: user_repository
description: Dart package which manages the user domain.
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  equatable: ^2.0.0
  pocketbase_provider:
    path: ../pocketbase_provider
  pocketbase: ^0.21.0

dev_dependencies:
  very_good_analysis: ^6.0.0
